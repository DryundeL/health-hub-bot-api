# ============================================
# HealthHub Bot API - Пример конфигурации
# ============================================
# Скопируйте этот файл в .env и заполните значениями для вашего окружения
# cp env.example .env

# ============================================
# БАЗА ДАННЫХ
# ============================================
# Вариант 1: Использование отдельных переменных (рекомендуется)
DB_HOST=localhost
DB_PORT=5432
DB_USER=healthhub_user
DB_PASSWORD=your_secure_password
DB_NAME=healthhub
DB_SSLMODE=disable
DB_TIMEZONE=UTC

# Вариант 2: Использование полной строки подключения (альтернатива)
# Если задан DATABASE_URL, отдельные переменные DB_* игнорируются
# DATABASE_URL=postgres://user:password@localhost:5432/healthhub?sslmode=disable&TimeZone=UTC

# Настройки пула соединений
DATABASE_MAX_OPEN_CONNS=25
DATABASE_MAX_IDLE_CONNS=5

# Уровень логирования GORM: silent, error, warn, info
DATABASE_LOG_LEVEL=info

# ============================================
# СЕРВЕР
# ============================================
PORT=8080
HOST=
# Порт для Docker Compose (внешний порт приложения)
APP_PORT=8080

# ============================================
# REDIS
# ============================================
REDIS_HOST=localhost
REDIS_PORT=6379

# ============================================
# TELEGRAM
# ============================================
TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here

# ============================================
# ХРАНИЛИЩЕ ФАЙЛОВ
# ============================================
# Тип хранилища: "local" или "s3"
STORAGE_TYPE=local

# Путь для локального хранилища (используется если STORAGE_TYPE=local)
STORAGE_PATH=./storage

# Настройки AWS S3 (используются если STORAGE_TYPE=s3)
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_REGION=us-east-1
S3_BUCKET=

